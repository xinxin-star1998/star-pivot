# ============================================
# 前端构建与 Lint CI
# ============================================
# 触发条件：push/PR 到 main/develop（仅前端文件变动），或手动触发
# 功能：安装依赖 → ESLint 检查 → TypeScript 类型检查 → 构建生产包
name: Frontend CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'star-pivot-ui/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'star-pivot-ui/**'
  workflow_dispatch:

jobs:
  build:
    name: Lint & Build (Node 20)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: star-pivot-ui

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('star-pivot-ui/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        run: pnpm lint

      - name: Build for production
        run: pnpm build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: frontend-dist
          path: star-pivot-ui/dist/
          retention-days: 7
