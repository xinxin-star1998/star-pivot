---
description: StarPivot 项目的核心通用规则（中文交流与基础编码规范）
alwaysApply: true
---

# StarPivot 项目核心规则

## 沟通与文档

- 所有与 AI 的对话与输出统一使用 **中文**。
- 代码中的 **注释、文档字符串、README 说明** 优先使用 **中文**，如需英文术语可中英文混排。
- 回答中避免使用表情符号，除非用户明确要求。

## 代码风格与质量

- **保持现有项目风格**：在修改或新增代码时，遵循所在文件/模块原有的书写风格与框架用法。
- **优先可读性**：变量名、方法名要表达业务含义，避免无意义缩写。
- **严格类型**：在 TypeScript、Java 等强类型语言中，避免随意使用 `any` / 原始 `Object` / 原始集合，偏向显式类型与统一模型。
- **错误处理明确**：
  - 后端：捕获异常时应记录日志，返回统一的业务错误码与提示。
  - 前端：对接口错误统一做提示处理（如 message 提示），避免静默失败。

## 注释与实现说明

- 关键业务逻辑、权限控制、复杂 SQL/查询、易踩坑部分，**必须配套中文注释** 简要说明“为什么这样做”。
- 对公共方法、工具类、基础组件，优先补充方法级/组件级中文说明（用途、入参出参、注意事项）。

## 前后端协作约定（高层）

- **接口契约稳定**：修改接口签名、出入参结构或权限控制时，应同步更新：
  - 后端 Controller / Service 注释
  - 对应前端 API 调用封装
  - 如有必要，更新 `doc/` 目录下相关说明。
- **权限与路由**：
  - 涉及菜单、路由、数据权限等改动时，优先查阅现有实现，遵循当前设计模式与命名习惯。
  - 新增权限点时，命名要与现有权限编码风格保持一致，并写明用途。

## AI 使用约定

- 在改动代码前，应先 **阅读相关文件上下文**，避免拍脑袋重构或与现有约定冲突。
- 对于存在多种实现方案的需求，AI 应先给出 **简要中文实现方案对比**，再落地具体改动。
- 如用户已有明确个人偏好（如中文注释、特定技术栈习惯），AI 改动时必须优先遵守这些偏好。

