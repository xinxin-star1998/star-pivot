<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.star.pivot.system.mapper.SysUserMapper">

    <resultMap type="com.star.pivot.system.domain.entity.SysUser" id="SysUserMap">
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="deptId" column="dept_id" jdbcType="INTEGER"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="nickName" column="nick_name" jdbcType="VARCHAR"/>
        <result property="userType" column="user_type" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="phonenumber" column="phonenumber" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="avatar" column="avatar" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="delFlag" column="del_flag" jdbcType="VARCHAR"/>
        <result property="loginIp" column="login_ip" jdbcType="VARCHAR"/>
        <result property="loginDate" column="login_date" jdbcType="TIMESTAMP"/>
        <result property="pwdUpdateDate" column="pwd_update_date" jdbcType="TIMESTAMP"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="selectPageList" resultMap="SysUserMap">
        select 
            u.user_id,
            u.dept_id,
            u.user_name,
            u.nick_name,
            u.user_type,
            u.email,
            u.phonenumber,
            u.sex,
            u.avatar,
            u.password,
            u.status,
            u.login_ip,
            u.login_date,
            u.pwd_update_date,
            u.create_by,
            u.create_time,
            u.update_by,
            u.update_time,
            u.del_flag,
            u.remark,
            r.role_name
        from sys_user u
        left join sys_user_role ur on u.user_id = ur.user_id
        left join sys_role r on r.role_id = ur.role_id
        <where>
            u.del_flag = '0'
            <if test="userReqBo.userName != null and userReqBo.userName != ''">
                and u.user_name like concat('%',#{userReqBo.userName},'%')
            </if>
            <if test="userReqBo.nickName != null and userReqBo.nickName != ''">
                and u.nick_name like concat('%',#{userReqBo.nickName},'%')
            </if>
        </where>
        order by u.create_time desc
    </select>
    <select id="getRolesByUserId" resultType="com.star.pivot.system.domain.entity.SysRole">
        SELECT r.*
        FROM sys_role r
                 INNER JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.del_flag = '0'
          AND (r.status = '0' OR r.role_key = 'admin')
        ORDER BY r.create_time desc
    </select>
     <select id="getMenuByUserId" resultType="com.star.pivot.system.domain.entity.SysMenu">
         SELECT DISTINCT p.*
         FROM sys_menu p
                  INNER JOIN sys_role_menu rp ON p.menu_id = rp.menu_id
                  INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
         WHERE ur.user_id = #{userId}
           AND p.visible = '0'
           AND p.status = '0'
        ORDER BY p.order_num desc
    </select>

</mapper>