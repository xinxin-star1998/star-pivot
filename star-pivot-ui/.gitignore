node_modules
.DS_Store
dist
dist-ssr
*.local
.cursorrules

# 环境变量文件（不忽略 .env.example）
.env
.env.local
.env.*.local

# Auto-generated files (committed for CI vue-tsc; regenerate via `pnpm dev` or `pnpm build`)
.auto-import.json
